/**
 * 7-segment decoder for segment f
 * Student: Mingjiang Bai (201860153)
 * Group: 13, Code: 0521634
 */
CHIP DecoderSegment201860153 {
    IN D, C, B, A;
    OUT f;

    PARTS:
    // Implement f = D + C + B'·A + B·A'
    Not(in=B, out=notB);
    Not(in=A, out=notA);
    And(a=notB, b=A, out=term1);  // B'·A
    And(a=B, b=notA, out=term2);   // B·A'
    Or(a=D, b=C, out=term3);
    Or(a=term1, b=term2, out=term4);
    Or(a=term3, b=term4, out=f);
}
